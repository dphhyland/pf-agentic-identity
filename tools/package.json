{
  "name": "pf-agentic-identity-tools",
  "private": true,
  "description": "What tools/build-showcase-docs.mjs needs: a pinned Markdown renderer, so the showcase's documents render the same on every machine.",
  "type": "module",
  "dependencies": {
    "marked": "12.0.2"
  }
}
