<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.pingidentity.ps.oidf</groupId>
    <artifactId>instance-registry-datasource</artifactId>
    <version>0.1.0</version>
    <packaging>jar</packaging>

    <name>instance-registry-datasource</name>
    <description>A PingFederate CustomDataSourceDriver over the agent instance registry. Lets an access
        token mapping resolve a pseudonymous instance identifier to its owner, status, device compliance
        and user-verification recency at issuance time — the point where revocation and the time-box
        actually bite.</description>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>17</maven.compiler.release>
        <version.jackson>2.17.1</version.jackson>
        <version.junit.jupiter>5.10.2</version.junit.jupiter>
        <version.postgresql>42.7.3</version.postgresql>
        <version.h2>2.2.224</version.h2>
    </properties>

    <dependencies>
        <dependency><groupId>com.pingidentity.ps.oidf</groupId><artifactId>device-instance</artifactId><version>0.1.0</version></dependency>
        <dependency><groupId>pingfederate</groupId><artifactId>pf-protocolengine</artifactId><version>13.0.0.3</version><scope>provided</scope></dependency>
        <dependency><groupId>com.pingidentity.pingfederate</groupId><artifactId>pingfederate-sdk</artifactId><version>13.0.0.3</version><scope>provided</scope></dependency>
        <!-- PF supplies a driver on its own classpath; bundling a second copy risks a LinkageError,
             the same trap the jose4j exclusion in assemble-pf-runtime-war.sh documents. -->
        <dependency><groupId>org.postgresql</groupId><artifactId>postgresql</artifactId><version>${version.postgresql}</version><scope>provided</scope></dependency>

        <dependency><groupId>org.junit.jupiter</groupId><artifactId>junit-jupiter</artifactId><version>${version.junit.jupiter}</version><scope>test</scope></dependency>
        <dependency><groupId>com.h2database</groupId><artifactId>h2</artifactId><version>${version.h2}</version><scope>test</scope></dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin><groupId>org.apache.maven.plugins</groupId><artifactId>maven-compiler-plugin</artifactId><version>3.13.0</version></plugin>
            <plugin><groupId>org.apache.maven.plugins</groupId><artifactId>maven-surefire-plugin</artifactId><version>3.2.5</version></plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.3.0</version>
                <configuration>
                    <!-- PF discovers plugin jars by filename prefix; without pf.plugins. it is ignored
                         silently, which is one of the ways this fails with no error at all. -->
                    <finalName>pf.plugins.instance-registry-datasource</finalName>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
